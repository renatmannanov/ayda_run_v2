# Ayda Run — Обновления экранов клубов и групп

## Что добавлено

### 1. Видимость в подзаголовке

**Файл:** `ayda-run-club-group-detail-v2.jsx`  
**Поиск:** `visibility`

```
🏆 SRG Almaty                    [TG]
   80 участников · 3 групп · 🌐 Публичный
```

| Тип | Публичный | Закрытый |
|-----|-----------|----------|
| Клуб | 🌐 Публичный | 🔒 Закрытый |
| Группа | 🌐 Публичная | 🏆 {Название клуба} |

---

### 2. Виды спорта

**Файл:** `ayda-run-club-group-detail-v2.jsx`  
**Поиск:** `sportTypes`

Иконки под подзаголовком:
```
🏆 SRG Almaty
   80 участников · 🌐 Публичный
   🏃 ⛰️ 🥾
```

Типы: `run`, `trail`, `hike`, `bike`, `yoga`, `workout`, `swim`

---

### 3. Ссылки (Links)

**Файл:** `ayda-run-club-group-detail-v2.jsx`  
**Поиск:** `linkIcons`, `LinkItem`

```
Ссылки                  + Добавить
📱 Общий чат                      →
🏃 Strava клуб                    →
📷 Instagram                      →
```

| Тип | Иконка |
|-----|--------|
| telegram | 📱 |
| strava | 🏃 |
| instagram | 📷 |
| excel | 📊 |
| youtube | 🎬 |
| website | 🌐 |
| other | 🔗 |

---

### 4. Отметка посещаемости

**Файл:** `ayda-run-activity-detail.jsx`  
**Поиск:** `AttendancePopup`, `showAttendance`

**Когда:** После окончания тренировки (status = 'awaiting')

**Кто видит:** Организатор (isOrganizer = true)

**Bottom bar:**
```
[📋 Отметить посещение  3/8]
```

**Popup:**
```
┌─────────────────────────────────┐
│ Отметка посещения            ✕  │
├─────────────────────────────────┤
│ ████████░░░░              6/8   │
├─────────────────────────────────┤
│ 👩 Анна            [✓]          │
│ 👨 Марат           [✓]          │
│ 👦 Дима            [ ]          │
├─────────────────────────────────┤
│ + Добавить участника            │
├─────────────────────────────────┤
│          [Сохранить]            │
└─────────────────────────────────┘
```

**Логика тапов:**
- 1й тап: `null → true` (✓ зелёный)
- 2й тап: `true → false` (✕ серый)  
- 3й тап: `false → null` (пусто)

---

## Модели данных

### Club
```typescript
{
  sports: string[];              // ['run', 'trail', 'hike']
  visibility: 'public' | 'private';
  access: 'open' | 'request';
  links: Link[];
}
```

### Group  
```typescript
{
  sports?: string[];             // опционально
  visibility: 'public' | 'club';
  visibilityClubName?: string;   // если visibility = 'club'
  access: 'open' | 'request';
  links: Link[];
}
```

### Link
```typescript
{
  id: string;
  type: 'telegram' | 'strava' | 'instagram' | 'excel' | 'youtube' | 'website' | 'other';
  label: string;
  url: string;
}
```

### Participant (с отметкой)
```typescript
{
  id, name, avatar,
  attended: true | false | null
}
```
