# Ayda Run — Home Screen: Изменения дизайна

## Референсный файл
`/ayda-run-home-compact.jsx`

---

## 1. Компактные пустые дни

**Было:** Под каждым днём текст "В этот день нет активностей" на отдельной строке.

**Стало:** Всё в одну строку: `Пн —— нет активностей —— 0`

**Где в коде:** Компонент `DayRow`

```jsx
{/* "нет активностей" - absolutely centered */}
{!hasActivities && (
  <span className="absolute left-1/2 -translate-x-1/2 text-xs text-gray-400 bg-white px-2">
    нет активностей
  </span>
)}
```

**Ключевое:**
- `absolute left-1/2 -translate-x-1/2` — текст по центру экрана
- `bg-white px-2` — белый фон чтобы перекрыть линию
- `text-gray-400` — цвет текста
- Линии слева и справа: `flex-1 border-b border-gray-200`

---

## 2. Переключатель недель внизу

**Было:** Блок "Текущая неделя" вверху под хедером.

**Стало:** Блок внизу, над навигацией (ближе к пальцу).

**Где в коде:** Секция `{/* Week Navigation - Bottom Bar Style */}`

**Структура:**
```
┌─────────────────────────────────┐
│  <     Текущая неделя       >   │
│  <<    22 дек. - 28 дек.   >>   │
└─────────────────────────────────┘
```

**Стили блока:**
```jsx
<div className="bg-gray-100 rounded-xl px-4 py-3">
```

---

## 3. Двойные стрелки для возврата к текущей

**Логика:**
- `<` — назад на 1 неделю (всегда активна)
- `>` — вперёд на 1 неделю (всегда активна)
- `<<` — к текущей (активна когда в будущем)
- `>>` — к текущей (активна когда в прошлом)

**Где в коде:** Внутри блока Week Navigation

```jsx
{/* << - к текущей, активна когда weekOffset > 0 (в будущем) */}
<button 
  onClick={() => setWeekOffset(0)}
  className={`p-1 transition-colors ${weekOffset > 0 ? 'text-gray-400 hover:text-gray-600' : 'text-gray-200'}`}
  disabled={weekOffset <= 0}
>
  <svg className="w-4 h-4">
    <path d="M11 19l-7-7 7-7M18 19l-7-7 7-7" /> {/* двойная стрелка влево */}
  </svg>
</button>

{/* >> - к текущей, активна когда weekOffset < 0 (в прошлом) */}
<button 
  onClick={() => setWeekOffset(0)}
  className={`p-1 transition-colors ${weekOffset < 0 ? 'text-gray-400 hover:text-gray-600' : 'text-gray-200'}`}
  disabled={weekOffset >= 0}
>
  <svg className="w-4 h-4">
    <path d="M13 5l7 7-7 7M6 5l7 7-7 7" /> {/* двойная стрелка вправо */}
  </svg>
</button>
```

**Размеры:**
- Одинарные стрелки: `w-5 h-5`
- Двойные стрелки: `w-4 h-4` (меньше)

---

## 4. Свайп для переключения недель

**Где в коде:** Хуки и обработчики в начале компонента `AydaRunHome`

```jsx
const [touchStart, setTouchStart] = useState(null);
const [touchEnd, setTouchEnd] = useState(null);

const minSwipeDistance = 50;

const onTouchStart = (e) => {
  setTouchEnd(null);
  setTouchStart(e.targetTouches[0].clientX);
};

const onTouchMove = (e) => {
  setTouchEnd(e.targetTouches[0].clientX);
};

const onTouchEnd = () => {
  if (!touchStart || !touchEnd) return;
  const distance = touchStart - touchEnd;
  const isLeftSwipe = distance > minSwipeDistance;
  const isRightSwipe = distance < -minSwipeDistance;
  
  if (isLeftSwipe) setWeekOffset(prev => prev + 1);
  if (isRightSwipe) setWeekOffset(prev => prev - 1);
};
```

**Применение:** На списке дней и на блоке недели
```jsx
<div 
  onTouchStart={onTouchStart}
  onTouchMove={onTouchMove}
  onTouchEnd={onTouchEnd}
>
```

---

## 5. Динамический лейбл недели

**Где в коде:** Функция `getWeekLabel()`

```jsx
const getWeekLabel = () => {
  if (weekOffset === 0) return 'Текущая неделя';
  if (weekOffset === 1) return 'Следующая неделя';
  if (weekOffset === -1) return 'Прошлая неделя';
  if (weekOffset > 1) return `Через ${weekOffset} нед.`;
  return `${Math.abs(weekOffset)} нед. назад`;
};
```

---

## Структура экрана (итог)

```
┌─────────────────────────────────┐
│ Мои / Все                     2 │  ← Header
├─────────────────────────────────┤
│ Сегодня —— нет активностей —— 0 │  ← DayRow (compact)
│ Вт ——————— нет активностей —— 0 │
│ ...                             │
│ Сб ——————————————————————————— 2│
│   [ActivityCard]                │
│   [ActivityCard]                │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │  <    Текущая неделя    >   │ │  ← Week Navigation (swipeable)
│ │  <<   22 дек. - 28 дек. >>  │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│  🏠    👥    👤    [+]          │  ← Bottom Nav
└─────────────────────────────────┘
```
