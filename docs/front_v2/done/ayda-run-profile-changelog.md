# Ayda Run — Профиль пользователя

## Референсный файл
`/ayda-run-profile.jsx`

---

## Структура экранов

```
Профиль → Статистика (отдельный экран)
        → Настройки (отдельный экран)
```

---

## 1. Профиль (главный экран)

### Хедер профиля

```
┌─────────────────────────────────┐
│  [RM]   Renat Mannanov          │
│  80px   @ray_mann               │
│         🏃 🚴                    │
│         [S] strava.com/...   →  │
└─────────────────────────────────┘
```

**Аватар:**
- Размер: `w-20 h-20` (80px)
- Инициалы или фото (настраивается)
- Цвет: `bg-indigo-500`

**Strava:**
- Есть: ссылка `[S] strava.com/athletes/...`
- Нет: кнопка `[S] Добавить Strava` (оранжевый текст)

**Где в коде:** Секция `{/* Profile Header */}`

---

### Клубы и группы

```
Клубы и группы (4)
[🏆]    [CR]    [ГБ]    [TN]
 SRG   Club..  Горные  Trail..
```

- Горизонтальный скролл
- Аватар: `w-12 h-12` (48px)
- Название: max ~8 символов, `truncate`

**Где в коде:** Компонент `ClubGroupCard`

---

### Ссылки на подэкраны

```
📊 Статистика                83% →
⚙️ Настройки                    →
```

**Где в коде:** Компонент `StatisticsPreview`

---

## 2. Статистика (отдельный экран)

### Табы периодов

```
[ Месяц ] [ Квартал ] [ Год ] [ Всё время ]
```

**Где в коде:** Компонент `PeriodTabs`

---

### Секции статистики

**Записался / Участвовал:**
```
87 / 94
████████████░░░░  93%
```

**По клубам и группам:**
```
[🏆] SRG Almaty       42/45
     ████████████░░░
```

**По видам спорта:**
```
🏃 Бег              62
   ████████████████░░░░
```

**Часто тренируюсь с:**
```
👩 Анна      24 совместных
👨 Марат     18 совместных
```

**Где в коде:** Компонент `StatisticsPage`

---

### Прогресс-бары

```jsx
// Приглушённый стиль
<div className="h-2 bg-gray-100 rounded-full">
  <div className="h-full bg-gray-300 rounded-full" />
</div>
```

**Где в коде:** Компонент `ProgressBar`

---

## 3. Настройки

### Показывать фото

```
Показывать фото           [●    ]
Вместо инициалов в аватарке
```

- ON: фото из Telegram
- OFF: инициалы на цветном фоне

**Где в коде:** `SettingsPage` → toggle `showPhoto`

---

### Strava

**Подключена:**
```
[S] strava.com/athletes/renat    Отвязать
```

**Не подключена:**
```
[ Подключить Strava ]  (оранжевая кнопка)
```

**Где в коде:** `SettingsPage` → секция Strava

---

## Структуры данных

### User
```typescript
interface User {
  name: string;
  username: string;       // @ray_mann
  initials: string;       // RM
  photo: string | null;   // URL или null
  sports: string[];       // ["🏃", "🚴"]
  strava: string | null;  // strava.com/athletes/...
  showPhoto: boolean;
}
```

### Club/Group
```typescript
interface ClubGroup {
  id: number;
  type: 'club' | 'group';
  name: string;
  shortName: string;      // Для инициалов
  avatar: string | null;  // Эмодзи или URL
}
```

### Statistics (по периоду)
```typescript
interface PeriodStats {
  registered: number;
  attended: number;
  clubs: Array<{
    name: string;
    avatar?: string;
    initials?: string;
    registered: number;
    attended: number;
  }>;
  sports: Array<{
    icon: string;
    name: string;
    count: number;
  }>;
  people: Array<{
    name: string;
    avatar: string;
    count: number;
  }>;
}
```

---

## Навигация между экранами

```jsx
const [currentPage, setCurrentPage] = useState('profile');
// 'profile' | 'settings' | 'statistics'

// Переход
setCurrentPage('statistics')

// Назад
onBack={() => setCurrentPage('profile')}
```

---

## Чеклист

### Frontend
- [x] Компактный хедер с большим аватаром
- [x] Strava ссылка / призыв добавить
- [x] Клубы и группы — аватарки в ряд
- [x] Статистика — отдельный экран
- [x] Табы периодов (месяц/квартал/год/всё)
- [x] Приглушённые прогресс-бары
- [x] Настройки — toggle фото
- [x] Настройки — Strava подключение

### Backend
- [ ] Endpoint GET /api/users/me/stats?period=month
- [ ] Агрегация по клубам/группам
- [ ] Агрегация по видам спорта
- [ ] Подсчёт совместных тренировок
- [ ] Strava OAuth интеграция
