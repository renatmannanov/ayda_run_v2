# Ayda Run — Фильтры по видам спорта

## Референсный файл
`/ayda-run-home-compact.jsx`

---

## Структура

```
┌─────────────────────────────────────────┐
│ Мои / Все   [🏃] [⛰️] [🚴] [+3]      2 │
└─────────────────────────────────────────┘
```

---

## Хедер с фильтрами

### Расположение
Фильтры в хедере между "Мои/Все" и счётчиком активностей.

### Видимые чипы
Показываем первые N видов спорта, остальные скрыты за `+N`.

```javascript
const VISIBLE_SPORTS = 3; // Настраивается
```

**Где в коде:** Константа `VISIBLE_SPORTS`

---

### Чип вида спорта

```jsx
<button className={`flex items-center gap-1 px-2 py-1 rounded-lg text-xs ${
  isSelected 
    ? 'bg-gray-800 text-white' 
    : 'bg-gray-100 text-gray-600'
}`}>
  <span>{sport.icon}</span>
</button>
```

**Где в коде:** Компонент `SportChip`

---

### Кнопка "+N"

- Показывает количество скрытых видов
- Становится тёмной если выбран скрытый вид
- Клик открывает попап

```jsx
hasHiddenSelected = selectedSports.some(id => 
  !visibleSports.find(s => s.id === id)
);
```

**Где в коде:** В хедере, секция `{/* +N button */}`

---

## Попап выбора

```
┌─────────────────────────────────────────┐
│ Виды спорта                          ✕  │
├─────────────────────────────────────────┤
│ [🏃 Бег] [⛰️ Трейл] [🚴 Вело]          │
│ [🧘 Йога] [🥾 Хайкинг] [✕ Сбросить]    │
└─────────────────────────────────────────┘
```

### Пилюли

```jsx
<button className={`flex items-center gap-1.5 px-3 py-2 rounded-full text-sm ${
  isSelected
    ? 'bg-gray-800 text-white'
    : 'bg-gray-100 text-gray-600'
}`}>
  <span>{sport.icon}</span>
  <span>{sport.name}</span>
</button>
```

### Кнопка "Сбросить"

- В том же ряду что и пилюли (flex-wrap)
- Появляется только когда что-то выбрано
- Иконка крестика + текст

```jsx
<button className="flex items-center gap-1.5 px-3 py-2 rounded-full text-sm text-gray-400">
  <svg>✕</svg>
  <span>Сбросить</span>
</button>
```

**Где в коде:** Компонент `SportFilterPopup`

---

## Данные

### Виды спорта

```javascript
const sportTypes = [
  { id: 'run', icon: '🏃', name: 'Бег' },
  { id: 'trail', icon: '⛰️', name: 'Трейл' },
  { id: 'bike', icon: '🚴', name: 'Вело' },
  { id: 'yoga', icon: '🧘', name: 'Йога' },
  { id: 'hike', icon: '🥾', name: 'Хайкинг' },
  { id: 'swim', icon: '🏊', name: 'Плавание' },
];
```

**Где в коде:** Константа `sportTypes`

---

### State

```javascript
const [selectedSports, setSelectedSports] = useState([]); // массив id
const [showSportPopup, setShowSportPopup] = useState(false);
```

---

### Handlers

```javascript
// Переключить выбор
const toggleSport = (sportId) => {
  setSelectedSports(prev => 
    prev.includes(sportId) 
      ? prev.filter(id => id !== sportId)
      : [...prev, sportId]
  );
};

// Сбросить все
const clearSportFilters = () => {
  setSelectedSports([]);
};
```

**Где в коде:** В `AydaRunHome`, секция `// Sport filter handlers`

---

## Логика фильтрации

Множественный выбор — активность показывается если её вид входит в `selectedSports`.

```javascript
// Пример фильтрации активностей
const filteredActivities = activities.filter(activity => 
  selectedSports.length === 0 || 
  selectedSports.includes(activity.sportType)
);
```

---

## Стили

| Элемент | Не выбран | Выбран |
|---------|-----------|--------|
| Чип в хедере | `bg-gray-100 text-gray-600` | `bg-gray-800 text-white` |
| Пилюля в попапе | `bg-gray-100 text-gray-600` | `bg-gray-800 text-white` |
| Кнопка +N | `bg-gray-100 text-gray-500` | `bg-gray-800 text-white` |
| Сбросить | `text-gray-400` | — |

---

## Чеклист

### Frontend
- [x] Чипы видов спорта в хедере
- [x] Кнопка "+N" для скрытых видов
- [x] Попап с пилюлями
- [x] Множественный выбор
- [x] Кнопка "Сбросить" в ряду с пилюлями
- [ ] Фильтрация списка активностей

### Backend
- [ ] Поле `sportType` в модели Activity
- [ ] Фильтр по видам спорта в API
- [ ] Endpoint GET /api/sport-types
