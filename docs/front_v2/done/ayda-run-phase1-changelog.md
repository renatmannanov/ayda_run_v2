# Ayda Run — Фаза 1: Изменения дизайна активностей

## Обзор

Документ описывает изменения в дизайне карточек активностей и экрана деталей активности. Референсные файлы содержат готовый код для реализации.

**Референсные файлы:**
- `/ayda-run-activity-list.jsx` — список активностей (карточки)
- `/ayda-run-activity-detail.jsx` — детали активности

---

## 1. Карточка активности (ActivityCard)

### 1.1 Организатор активности

**Было:** Не отображался

**Стало:** Вторая строка под названием

**Логика отображения:**
- Клуб/группа: `SRG Almaty · Горные бегуны`
- Пользователь: `организатор 👨 Марат`

**Где в коде:** `ActivityCard` → функция `getOrganizerText()`

```jsx
const getOrganizerText = () => {
  if (activity.organizer.type === 'user') {
    return (
      <span className="flex items-center gap-1">
        <span className="text-gray-400">организатор</span>
        <span>{activity.organizer.avatar}</span>
        <span>{activity.organizer.name}</span>
      </span>
    );
  }
  return `${activity.organizer.club} · ${activity.organizer.group}`;
};
```

**Структура данных:**
```typescript
organizer: 
  | { type: 'club'; club: string; group: string }
  | { type: 'user'; name: string; avatar: string }
```

---

### 1.2 Дистанция и набор высоты

**Было:** Всегда отображались `км · ↗ м`

**Стало:** Скрываются если пустые (для йоги, зала и т.д.)

**Где в коде:** `ActivityCard` → функция `getDistanceText()` + условный рендеринг

```jsx
const getDistanceText = () => {
  if (!activity.distance && !activity.elevation) return null;
  const parts = [];
  if (activity.distance) parts.push(activity.distance);
  if (activity.elevation) parts.push(`↗ ${activity.elevation}`);
  return parts.join(' · ');
};

// В рендере:
{getDistanceText() && (
  <p className="text-sm text-gray-500 mb-2">{getDistanceText()}</p>
)}
```

---

### 1.3 Кнопка статуса записи (StatusButton)

**Было:** 
- Не записан: тонкий `+` в светлом круге
- Записан: залитый серый круг
- Был: залитый зелёный круг

**Стало:** Единый стиль — контурный круг с иконкой внутри

| Статус | Визуал | Код |
|--------|--------|-----|
| Не записан | Серая рамка + серый жирный `+` | `border-gray-400`, `strokeWidth={3}` |
| Записан | Серая рамка + серая жирная `✓` | `border-gray-400`, `strokeWidth={3}` |
| Был | Зелёная рамка + зелёная жирная `✓` | `border-green-500`, `strokeWidth={3}` |

**Где в коде:** Компонент `StatusButton`

```jsx
// Не записан
<button className="w-9 h-9 rounded-full border-[2.5px] border-gray-400 flex items-center justify-center text-gray-400">
  <svg className="w-5 h-5" strokeWidth={3}>
    <path d="M12 6v12m6-6H6" /> {/* Plus */}
  </svg>
</button>

// Записан
<div className="w-9 h-9 rounded-full border-[2.5px] border-gray-400 flex items-center justify-center">
  <svg className="w-5 h-5 text-gray-400" strokeWidth={3}>
    <path d="M5 13l4 4L19 7" /> {/* Checkmark */}
  </svg>
</div>

// Был
<div className="w-9 h-9 rounded-full border-[2.5px] border-green-500 flex items-center justify-center">
  <svg className="w-5 h-5 text-green-500" strokeWidth={3}>
    <path d="M5 13l4 4L19 7" /> {/* Checkmark */}
  </svg>
</div>
```

**Ключевые стили:**
- Размер круга: `w-9 h-9`
- Толщина рамки: `border-[2.5px]`
- Толщина иконки: `strokeWidth={3}`
- Размер иконки: `w-5 h-5`

---

### 1.4 Замочек для закрытых тренировок

**Было:** Эмодзи 🔒 в начале названия

**Стало:** SVG иконка замка слева от кнопки статуса

**Где в коде:** `StatusButton` → условие `isPrivate`

```jsx
{isPrivate && (
  <svg className="w-7 h-7 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path strokeLinecap="round" strokeLinejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
  </svg>
)}
```

**Расположение:** `flex items-center gap-1.5` — замок слева, кнопка справа

---

## 2. Детали активности (ActivityDetail)

### 2.1 Структура экрана

**Новый порядок секций:**
1. Название + иконка спорта
2. Характеристики (дата, место, дистанция)
3. Ссылка "Скачать gpx трек" (если есть)
4. Описание
5. Организатор (клуб/группа или юзер)
6. Участники (аватарки + счётчик)
7. **Блок организатора** (редактировать, удалить) — внизу!
8. Кнопка действия (записаться / иду / заявка)

**Где в коде:** Компонент `ActivityDetail` → JSX структура

---

### 2.2 Скачать GPX трек

**Было:** Блок "GPX Route" с обводкой

**Стало:** Простая текстовая ссылка под характеристиками

**Где в коде:** После блока характеристик

```jsx
{activity.hasGpx && (
  <div className="mt-3 mb-2">
    <button className="text-xs text-gray-500 underline hover:text-gray-700 transition-colors">
      Скачать gpx трек
    </button>
  </div>
)}
```

---

### 2.3 Организатор — отображение

**Клуб/группа:**
```jsx
<p className="text-sm text-gray-700">
  <span className="cursor-pointer hover:underline">🏆 {activity.organizer.club}</span>
  <span className="text-gray-400"> / </span>
  <span className="cursor-pointer hover:underline">{activity.organizer.group}</span>
</p>
```

**Пользователь:** (аватарка увеличена до `text-xl`)
```jsx
<p className="text-sm text-gray-700 flex items-center gap-1">
  <span className="text-gray-400">организатор</span>
  <span className="text-xl cursor-pointer hover:opacity-70">{activity.organizer.avatar}</span>
  <span className="cursor-pointer hover:underline">{activity.organizer.name}</span>
</p>
```

**Где в коде:** Функция `getOrganizerDisplay()`

**Кликабельность:**
- Клуб → `/club/{id}`
- Группа → `/group/{id}`
- Пользователь → профиль юзера

---

### 2.4 Блок организатора (админские действия)

**Было:** Кнопка "Орг" в шапке

**Стало:** Блок внизу карточки (ближе к пальцу на мобилке)

**Где в коде:** Условный блок `{isOrganizer && (...)}`

```jsx
{isOrganizer && (
  <>
    <div className="border-t border-gray-200 my-4" />
    <div className="space-y-2">
      {!activity.hasGpx && (
        <button className="flex items-center gap-2 text-sm text-gray-700 hover:text-gray-900 py-1">
          <span>⚡</span>
          <span className="font-medium">Добавить GPX файл</span>
        </button>
      )}
      {activity.hasGpx && (
        <div className="flex items-center gap-2 text-sm text-gray-500 py-1">
          <span>📍</span>
          <span>GPX добавлен</span>
        </div>
      )}
      <button className="flex items-center gap-2 text-sm text-gray-500 hover:text-gray-700 py-1">
        <span>✏️</span>
        <span>Редактировать</span>
      </button>
      <button className="flex items-center gap-2 text-sm text-gray-500 hover:text-gray-700 py-1">
        <span>🗑</span>
        <span>Удалить</span>
      </button>
    </div>
  </>
)}
```

---

### 2.5 Нижняя панель действий

**Состояния:**

| Состояние | Левая часть | Правая часть |
|-----------|-------------|--------------|
| Не записан | — | `[Записаться]` большая кнопка |
| Записан | `Отменить` (серый мелкий) | `Иду!` + `[↗]` share |
| Закрыто, не записан | — | `[🔒 Подать заявку на участие]` |
| Закрыто, заявка | `Отменить` | `🔒 Заявка отправлена` |

**Где в коде:** Функции `getActionButton()` и `renderBottomBar()`

---

### 2.6 Поп-ап участников (ParticipantsPopup)

**Изменения:**

1. **Расположение:** Поп-ап появляется НАД кнопкой действия
2. **Кнопка действия:** Встроена в поп-ап внизу
3. **Layout строки участника:**
   - Слева: Аватарка + Имя + Виды спорта
   - Справа: Ярлык "Орг" + Strava

**Где в коде:** Компонент `ParticipantsPopup`

```jsx
<div className="flex items-center py-3 border-b border-gray-100 last:border-0">
  {/* Avatar */}
  <span className="text-2xl mr-3 flex-shrink-0">{p.avatar}</span>
  
  {/* Name + Sports - left side */}
  <div className="flex items-center gap-2 flex-1 min-w-0">
    <span className="text-sm text-gray-700">{p.name}</span>
    <div className="flex gap-0.5 flex-shrink-0">
      {p.sports.map((sport, i) => (
        <span key={i} className="text-sm">{sport}</span>
      ))}
    </div>
  </div>
  
  {/* Organizer badge - right side near Strava */}
  {p.isOrganizer && (
    <span className="text-xs text-gray-400 bg-gray-100 px-1.5 py-0.5 rounded mr-2">Орг</span>
  )}
  
  {/* Strava */}
  <StravaIcon url={p.stravaUrl} />
</div>
```

**Структура поп-апа:**
```
┌─────────────────────────────────────────┐
│  Участники · 8/20                   ✕   │
├─────────────────────────────────────────┤
│  👩 Анна      🏃⛰️              Орг [S]  │
│  👨 Марат     🏃🚴                  [S]  │
│  ...                                    │
├─────────────────────────────────────────┤
│         [ Записаться ]                  │  ← Кнопка действия здесь!
└─────────────────────────────────────────┘
```

**Ключевой код — кнопка в поп-апе:**
```jsx
<ParticipantsPopup 
  participants={...}
  total={activity.participants}
  max={activity.maxParticipants}
  onClose={() => setShowParticipants(false)}
  actionButton={getActionButton()}  // Передаём кнопку как prop
/>
```

---

## 3. Структуры данных

### Activity
```typescript
interface Activity {
  id: number;
  title: string;
  organizer: 
    | { type: 'club'; club: string; group: string }
    | { type: 'user'; name: string; avatar: string };
  date: string;        // "сб, 27 дек"
  time: string;        // "07:00"
  location: string;
  distance: string | null;    // "10 км" или null
  elevation: string | null;   // "850 м" или null
  sportIcon: string;   // "🏃" | "⛰️" | "🥾" | "🚴" | "🧘"
  participants: number;
  maxParticipants: number;
  status: 'none' | 'registered' | 'attended' | 'missed';
  isPrivate: boolean;
  hasGpx: boolean;
  description?: string;
}
```

### Participant
```typescript
interface Participant {
  id: number;
  name: string;
  avatar: string;      // Эмодзи или URL
  isOrganizer: boolean;
  sports: string[];    // ["🏃", "⛰️"]
  stravaUrl: string | null;
}
```

---

## 4. Цветовая схема

**Основа — монохром:**

| Элемент | Цвет |
|---------|------|
| Текст основной | `text-gray-800` |
| Текст вторичный | `text-gray-500` |
| Текст приглушённый | `text-gray-400` |
| Рамки | `border-gray-200` или `border-gray-400` |
| Кнопка primary | `bg-gray-800 text-white` |
| Статус "был" | `border-green-500 text-green-500` |
| Strava | `bg-orange-500` |

**Принцип:** Минимум ярких акцентов. Зелёный только для "был", оранжевый только для Strava.

---

## 5. Чеклист для реализации

### Карточка активности
- [ ] Добавить строку организатора под названием
- [ ] Скрывать км/м если пустые
- [ ] Заменить кнопку статуса на новый дизайн (контурный круг)
- [ ] Замочек — SVG слева от кнопки статуса

### Детали активности
- [ ] Убрать блок "GPX Route", добавить текстовую ссылку
- [ ] Организатор с увеличенной аватаркой для юзеров
- [ ] Блок админа переместить вниз
- [ ] Нижняя панель: "Отменить" слева, "Иду!" + share справа
- [ ] Для закрытых: "🔒 Подать заявку на участие"

### Поп-ап участников
- [ ] Layout: имя + спорты слева, Орг + Strava справа
- [ ] Поп-ап появляется над кнопкой действия
- [ ] Кнопка действия встроена в поп-ап

---

## 6. Референсы

Готовый код в файлах:
- `ayda-run-activity-list.jsx` — полный компонент списка
- `ayda-run-activity-detail.jsx` — полный компонент деталей

Открой файлы и используй как референс для стилей и логики.
