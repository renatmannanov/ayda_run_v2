# Phase 2 Testing Guide - Club/Group Invitations

**Дата:** 2025-12-18
**Статус:** Ready for Testing

## Что реализовано в Phase 2

### Flow 2A - Club Invitation
- Поддержка deep link: `/start club_UUID`
- Для **новых пользователей**: показывает информацию о клубе + полный онбординг
- Для **существующих пользователей**: сразу показывает приглашение с кнопками "Вступить" / "Не сейчас"
- Автоматическое присоединение к клубу после завершения онбординга
- Проверка на повторное вступление (если уже участник)

### Flow 2B - Group Invitation
- Поддержка deep link: `/start group_UUID`
- Для **новых пользователей**: показывает информацию о группе + полный онбординг
- Для **существующих пользователей**: сразу показывает приглашение с кнопками "Вступить" / "Не сейчас"
- Автоматическое присоединение к группе после завершения онбординга
- Проверка на повторное вступление (если уже участник)

## Файлы созданные/изменённые

### Новые файлы:
- `bot/invitation_handler.py` - обработчики кнопок join/decline для существующих пользователей

### Изменённые файлы:
- `bot/onboarding_handler.py`:
  - Добавлен парсинг deep link параметров (`club_UUID`, `group_UUID`)
  - Добавлена функция `handle_existing_user_invitation()` для существующих пользователей
  - Изменён welcome message для показа информации о клубе/группе при приглашении
  - Автоматическое присоединение к клубу/группе в `complete_onboarding()`

- `api_server.py`:
  - Добавлен импорт `join_invitation_handlers`
  - Зарегистрированы обработчики приглашений

## Как тестировать

### Предварительные требования:
1. База данных должна содержать:
   - Хотя бы 1 клуб (с UUID)
   - Хотя бы 1 группу (с UUID)

2. Бот должен быть запущен через `api_server.py`

### Сценарий 1: Новый пользователь + приглашение в клуб

1. Создай invitation link: `https://t.me/your_bot?start=club_{CLUB_UUID}`
2. Перейди по ссылке из **нового** Telegram аккаунта (который никогда не нажимал /start)
3. **Ожидаемое поведение:**
   - Показывается welcome message с информацией о клубе
   - Полный онбординг flow (consent → sports → role → intro)
   - После нажатия "Готово" → автоматически присоединяется к клубу
   - Показывается сообщение "Добро пожаловать в {club_name}! 🎉"
   - Кнопка WebApp с deep link: `{app_url}?startapp=club_{UUID}`

### Сценарий 2: Существующий пользователь + приглашение в клуб

1. Создай invitation link: `https://t.me/your_bot?start=club_{CLUB_UUID}`
2. Перейди по ссылке из **существующего** Telegram аккаунта (который уже прошёл онбординг)
3. **Ожидаемое поведение:**
   - Сразу показывается "С возвращением!" + информация о клубе
   - Две кнопки: "✅ Вступить" и "❌ Не сейчас"
   - При нажатии "Вступить":
     - Присоединяется к клубу
     - Показывается success message
     - Кнопка WebApp с deep link
   - При нажатии "Не сейчас":
     - Показывается "Хорошо! Ссылка будет работать..."
     - Кнопка "🔍 Посмотреть тренировки" (WebApp без deep link)

### Сценарий 3: Уже участник клуба

1. Перейди по ссылке `/start club_{UUID}` от пользователя, который **уже участник** этого клуба
2. **Ожидаемое поведение:**
   - Показывается "👋 Ты уже участник клуба {name}!"
   - Кнопка WebApp для открытия клуба

### Сценарий 4: Приглашение в группу

Повтори сценарии 1-3, но с group_UUID вместо club_UUID.

### Сценарий 5: Несуществующий клуб/группа

1. Перейди по ссылке `/start club_invalid_uuid`
2. **Ожидаемое поведение:**
   - Показывается "❌ Упс! Не могу найти этот клуб."
   - Предложение запросить новую ссылку

## Возможные проблемы

### Проблема 1: Не работает deep link
- **Проверь:** Формат UUID корректный?
- **Проверь:** Клуб/группа существует в БД?
- **Проверь:** `ClubStorage.get_club_preview()` возвращает данные?

### Проблема 2: Не добавляется в клуб/группу
- **Проверь:** Логи на ошибки в `add_member_to_club/group()`
- **Проверь:** Таблица `memberships` содержит записи?

### Проблема 3: WebApp не открывается
- **Проверь:** `settings.app_url` установлен корректно?
- **Проверь:** Формат WebApp URL: `{app_url}?startapp=club_{UUID}`

## Следующие шаги после тестирования

1. ✅ Если всё работает → коммит + push
2. ❌ Если есть баги → исправление + повторное тестирование
3. ➡️ Переход к Phase 3 - Organizer Flow (создание клубов)
